Application trigger stuff

To run integration test use the following command<br/>
```./gradlew test -Dgroups=it```
<br/>
@see also TestCategories class

Pass ```-Dgroups=it``` as an argument to IntellJ test
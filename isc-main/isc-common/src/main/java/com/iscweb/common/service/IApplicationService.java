package com.iscweb.common.service;

/**
 * An interface for all application services.
 *
 * WARNING: NEVER implement this interface directly unless you are doing it intentionally.
 * First interface you should consider implementing is {@link IApplicationSecuredService}
 * as it provides application security layer features by default.
 */
public interface IApplicationService {
    //@see WARNING above!
}
